fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.6.0
boto3>=1.34.0
websockets>=12.0
asyncpg>=0.29.0
# RAG는 별도 rag-svc(ECS) 서비스로 분리.
# orchestrator는 httpx로 POST /query 호출만 함 — chromadb 의존 제거.
# ML Decision Engine
scikit-learn>=1.3.0
lightgbm>=4.0.0
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=15.0.0   # CC map .parquet 파일 읽기용 (pandas의 엔진)
# Push 알림 (FCM)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json 환경변수로 초기화.
# 변수 없으면 fcm.init()는 graceful no-op → 푸시 미발송, 운영 흐름은 그대로 진행.
firebase-admin>=6.5.0
